const URL=process.env.SUPABASE_URL;const KEY=process.env.SUPABASE_SERVICE_ROLE_KEY;function ready(){if(!URL||!KEY)throw new Error('Database is not configured. Add SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY in Vercel.')}async function db(path,options={}){ready();const r=await fetch(`${URL}/rest/v1/${path}`,{...options,headers:{apikey:KEY,Authorization:`Bearer ${KEY}`,'Content-Type':'application/json',Prefer:'return=representation',...(options.headers||{})}});const text=await r.text();let data=text?JSON.parse(text):null;if(!r.ok)throw new Error(data?.message||data?.hint||'Database error');return data}function admin(req){const expected=process.env.ADMIN_PASSWORD;if(!expected)throw new Error('ADMIN_PASSWORD is not configured in Vercel.');if(req.headers['x-admin-password']!==expected){const e=new Error('Invalid admin password');e.status=401;throw e}}module.exports={db,admin};